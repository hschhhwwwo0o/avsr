import React, { Fragment, FunctionComponent } from "react";

const ArtCreateButton: FunctionComponent = () => {
  return (
    <Fragment>
      <button className="px-8 bg-white border-[#252525] border-t border-r border-b text-sm uppercase">Create</button>
    </Fragment>
  );
};

export default ArtCreateButton;
